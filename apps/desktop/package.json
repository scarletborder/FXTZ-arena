{
  "name": "desktop",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "",
    "build": "node ./scripts/build-frontend.mjs && node ./scripts/build-tauri.mjs",
    "build:portable": "node ./scripts/build-tauri.mjs",
    "publish-desktop": "pnpm build && node ./scripts/collect-artifacts.js",
    "publish-portable": "pnpm build:portable && node ./scripts/collect-artifacts.js"
  },
  "dependencies": {
    "frontend": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
