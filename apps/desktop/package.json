{
  "name": "desktop",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/build-frontend.mjs && tauri dev",
    "build": "node ./scripts/build-frontend.mjs && tauri build --no-bundle",
    "publish-desktop": "pnpm build && node ./scripts/collect-artifacts.js"
  },
  "dependencies": {
    "frontend": "workspace:*"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
